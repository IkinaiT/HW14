using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalStringVars
{
    public const string HorizontalAxis = "Horizontal";
    public const string JumpButton = "Jump";

    #region PlayerPrefsNames

    public const string SoundLevel = nameof(SoundLevel);
    public const string IsSoundsOn = nameof(IsSoundsOn);
    public const string CompletedLevels = nameof(CompletedLevels);

    #endregion
}
